[project]
name = "pyspark-iceberg-medallion-retail"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyarrow>=24.0.0",
    "pyiceberg>=0.11.1",
    "pyspark>=4.1.1",
    "python-dotenv>=1.2.2",
]
